# Android Setup Enviroment

Create the Enviroment for AOSP

The script is meant to be run as root

```bash
git clone https://github.com/TechPanelGM/AndroidBuildEnv.git && cd AndroidBuildEnv
chmod +x menu.sh
./menu.sh

```
    
